# 📄 SHORT_SPEC — ПАПАЖКХ

Версия: **v1.5.1** (2026-01-22)

Краткое техническое задание для стороннего разработчика.

Цель: дать понимание логики системы **без погружения во внутренний код**.

---

## 1. Общая идея

Система ведёт:
- начисления ЖКХ по месяцам;
- оплаты;
- задолженность и пеню;
- расчёт взыскиваемых сумм.

Фокус: **юридическая и финансовая корректность**.

---

## 2. Базовые правила (CRITICAL)

- одно начисление = один месяц;
- начисления нельзя редактировать вручную;
- новый абонент стартует с нулевой задолженности;
- долги и пени до даты начала расчёта не существуют.

---

## 3. Оплаты

Каждая оплата имеет:
- месяц строки;
- сумму;
- фактическую дату оплаты;
- источник;
- флаг `use_period`.


### payments_<LS> ledger (CRITICAL)

- Данные оплат/начислений абонента хранятся в LocalStorage ключе `payments_<LS>` (пример: `payments_0001`).
- Это **помесячная таблица (ledger)**, а не список событий платежей.
- В одном месяце допустимо несколько строк:
  - строка начисления/импорта (часто `is_imported=true` и/или `locked=true`) — **не редактируется вручную**;
  - одна или несколько строк ручной оплаты (создаются через UI «Добавить оплату»).
- Долг месяца считается по агрегированию строк месяца:
  - `accruedYM = max(accrued)` по строкам месяца
  - `paidYM = sum(paid)` по всем строкам месяца
  - `debtYM = max(0, accruedYM - paidYM)`
- Запрещена ретро‑перезапись: «оплата за прошлый период» не меняет фактическую дату оплаты и не переписывает прошлые месяцы.

### Оплата за период

- используется **только для расчёта пени**;
- не меняет основной долг;
- включается только вручную.

---

## 4. Импорт Excel (CRITICAL)

- Excel загружает **факты**, не интерпретации;
- импортированные оплаты:
  - автоматически получают источник;
  - не подлежат редактированию;
  - не могут быть удалены.

---

## 5. Расчёт долга

- долг считается нарастающим итогом;
- если оплаты превышают начисления — образуется аванс;
- аванс отображается отрицательным значением.

---

## 6. Расчёт взыскиваемой суммы

При выборе периода взыскания:
- расчёт начинается с нуля;
- учитываются только данные внутри периода.

---

## 7. Тарифы

- тарифы задаются только с начала месяца;
- выбор даты — месяц/год;
- один месяц — один тариф.

---

## 8. Что запрещено

🚫 менять расчёт долга без обновления LOGIC_SPEC;  
🚫 редактировать импортные оплаты;  
🚫 учитывать данные до даты начала расчёта;  
🚫 добавлять начисления вне месяца.

---

## 9. Эталон

Рабочий эталон логики:
```
jkh_site_full_v01.21.5.zip
```

Любые изменения допускаются только **поверх эталона**.
