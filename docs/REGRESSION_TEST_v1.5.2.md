# 🧪 REGRESSION_TEST v1.5.2

Ручные регрессионные тесты перед любым релизом/архивом.

## 0. Правило
Если любой тест провален — версию нельзя использовать для суда.

---

## 1. Новый абонент — нулевой старт (CRITICAL)
1) Создать абонента с `calcStartDate = 01.01.2025`.
2) Открыть месяцы до января 2025.

Ожидание:
- начисления = 0
- оплаты = 0
- задолженность = 0
- пеня = 0

---

## 2. Импорт Excel — блокировка оплат (CRITICAL)
1) Импортировать Excel с оплатами.
2) Открыть карточку абонента.

Ожидание:
- строки импорта помечены 📥
- сумма/дата/источник не редактируются
- удаление отсутствует

---

## 3. payments_<LS> — ledger в одном ключе (CRITICAL)
1) В одном месяце сделать: 1 начисление + 2 оплаты (любые).
Ожидание:
- строк в месяце > 1 — это нормально
- долг месяца считается как max(accrued) - sum(paid)

---

## 4. Оплата за период влияет только на пеню (CRITICAL)
1) На ручной оплате включить «за период» и задать период.
Ожидание:
- основной долг не меняется
- дата оплаты не меняется
- меняется только логика/назначение пени

---

## 5. Anti-retro (CRITICAL)
1) Поставить оплате дату фактическую (например, 2026-01-25).
2) Выставить «за период» на август 2025.
Ожидание:
- пеня не пересчитывается так, будто деньги пришли в августе 2025
- временная ось для пени = фактическая дата оплаты

---

## 6. Исключённые периоды = только пеня (CRITICAL)
1) Включить исключённый период, который перекрывает часть просрочки.
Ожидание:
- основной долг не меняется
- пеня за исключённые даты = 0

---

## 7. Карточка ↔ Справка для суда (CRITICAL)
1) Сформировать справку для суда на тот же период/дату, что и карточка.
Ожидание:
- итог по долгу совпадает
- итог по пени совпадает
- помесячные колонки справки считаются построчно (за месяц), без накопления

---

## 8. ES-modules запрещены (CRITICAL)
Открыть проект через `file://` и убедиться, что:
- нет `type="module"`
- нет ошибок CORS/Import
- расчёты работают

✅ Если все тесты пройдены — версия допустима.
