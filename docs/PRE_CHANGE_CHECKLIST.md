# 🧪 CHECKLIST перед любым изменением

Если хотя бы один пункт не выполняется — правку **нельзя** вносить.

1) Карточка абонента (UI) не меняется визуально и логически (или это осознанная версия/релиз)?
2) LOGIC_CHECKLIST v1.4 проходит полностью?
3) `payments_<LS>` остаётся ledger-таблицей, не «журналом событий»?
4) Anti-retro соблюдён (оплата за период не меняет дату оплаты и не переписывает прошлое)?
5) Исключённые периоды отключают только пеню (долг не меняется)?
6) Итог карточки и итог справки для суда совпадают на одинаковую дату?
7) Запись внесена в CRITICAL_CHANGELOG и поднята версия LOGIC_SPEC (если менялась логика)?

Рекомендация:
- Перед коммитом/архивом прогнать REGRESSION_TEST и сохранить скриншоты ключевых кейсов.
