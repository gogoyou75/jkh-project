# 🔒 CRITICAL — НЕ ТРОГАТЬ (ПАПАЖКХ)

Этот файл — «предохранитель». Его цель: чтобы исправления в одном месте
не ломали расчёты для суда и карточку абонента.

## НЕЛЬЗЯ без новой версии LOGIC_SPEC
- менять порядок подключений расчётных скриптов (calc_engine.js должен быть раньше)
- внедрять ES-modules (type="module", import/export)
- менять смысл `payments_<LS>` (ledger)
- менять anti-retro (запрет «переписать прошлое»)
- менять правило «исключённые периоды = только пеня»

## Если цифры не совпали
1) Считаем карточку абонента эталоном  
2) Исправляем производные (spravka_sud / отчёты), НЕ меняя карточку  
3) Фиксируем правку в CRITICAL_CHANGELOG

