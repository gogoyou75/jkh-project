# 🧪 REGRESSION_TEST

Ручные регрессионные тесты проекта **ПАПАЖКХ**.

Цель: убедиться, что новые изменения **не сломали критическую логику**.

Тесты выполняются:
- перед релизом;
- перед передачей проекта;
- перед формированием судебных и финансовых отчётов.

---

## 1. Новый абонент — нулевой старт (CRITICAL)

1. Создать абонента с `calcStartDate = 01.01.2025`.
2. Проверить месяцы до января 2025.

**Ожидание:**
- начисления = 0;
- оплаты = 0;
- задолженность = 0;
- пеня = 0.

---

## 2. Импорт Excel — блокировка оплат (CRITICAL)

1. Импортировать файл Excel с оплатами.
2. Открыть карточку абонента.

**Ожидание:**
- оплаты присутствуют;
- сумма, дата, источник недоступны для редактирования;
- кнопка удаления отсутствует;
- строка помечена значком 📥.

---

## 3. Оплата за период влияет только на пеню (CRITICAL)

1. Создать оплату.
2. Включить «указать за период».
3. Задать период.

**Ожидание:**
- основной долг не изменился;
- пеня пересчиталась;
- дата оплаты осталась фактической.

---

## 4. Расчёт взыскиваемой суммы за период (CRITICAL)

1. Выбрать период взыскания.
2. Выполнить расчёт.

**Ожидание:**
- расчёт начинается с нуля на начале периода;
- долг до периода не учитывается;
- считаются только начисления и оплаты внутри периода.

---

## 5. Аванс (отрицательная задолженность)

1. Внести оплату больше начисления.

**Ожидание:**
- задолженность отрицательная;
- отображается со знаком минус;
- выделена тёмно-красным цветом.

---

## 6. Тарифы — только начало месяца (CRITICAL)

1. Добавить тариф с датой середины месяца.

**Ожидание:**
- ввод невозможен;
- дата нормализуется к первому числу месяца.

---

## 7. Совместимость данных

1. Открыть старых абонентов.

**Ожидание:**
- данные отображаются корректно;
- автоматической миграции нет;
- расчёты не пересчитываются без команды.

---

✅ Если все тесты пройдены — версия считается допустимой к использованию.

---

## 8. Платежи в одном месяце: порядок строк, «пени» и «оплата за период» (CRITICAL)

Цель: не путать оператора отображением и гарантировать одинаковую логику в UI и отчёте.

### 8.1 Порядок строк внутри месяца
1) Открыть карточку абонента, где в одном и том же месяце есть:
- одна строка начисления (accrued > 0);
- минимум 2 оплаты (любая комбинация: Excel-импорт и ручная).

**Ожидание:**
- в группе одного месяца первой строкой идёт **начисление**;
- ниже идут **оплаты** (Excel и ручные), отсортированные по дате оплаты (новые выше).

### 8.2 «Пени» не показываются на строках оплат
1) В том же месяце посмотреть колонку **«по пени»**.

**Ожидание:**
- на строках оплат (paid > 0) в колонке «по пени» пусто;
- «пени» показываются на строке **начисления** (и именно там оператор видит итог за месяц).

### 8.3 «Оплата за период» сохраняется автоматически
1) На ручной оплате нажать «указать за период» → включить «за период».
2) Поменять период (месяц/год «с» и «по»).
3) Нажать F5 (обновить страницу) и снова открыть эту оплату.

**Ожидание:**
- выбранные period_from / period_to сохранены (без отдельной кнопки “Сохранить” в строке);
- перерасчёт/отображение долга не ломаются.

### 8.4 «Оплата за период» НЕ влияет на расчёт пени по времени
1) Для одной из оплат выставить «за период» на “август”, но дату оплаты оставить реальную (например, январь).
2) Сравнить «пени»/«итог» до и после установки периода.

**Ожидание:**
- пени считаются по **дате оплаты** и dueDate, а не по выбранному “за период”;
- изменение “за период” меняет только метку/назначение платежа (для отчётов/распределения), но не меняет временную ось начисления пени.
