# 🔐 CRITICAL_CHANGELOG.md

## v1.5 → v1.5.1 — 2026-01-22

### 🔴 CRITICAL: Архитектура оплат (payments ledger)

**Что зафиксировано**
- `payments_<LS>` (например, `payments_0001`) является **единым помесячным ledger**, а не списком событий платежей.
- В одном месяце допустимо **несколько строк**:
  - строка начисления (обычно импорт из Excel, `is_imported / locked`)
  - одна или несколько строк ручной оплаты (manual)

**Два пути оплаты**
1. **Импорт из Excel**
   - создаёт строки начисления/состояния месяца
   - строки могут быть `locked` и недоступны для ручного редактирования
2. **Ручная оплата через UI**
   - создаёт отдельные строки оплаты
   - сохраняется в тот же ключ `payments_<LS>`

**Ключевые правила**
- Ручная оплата **не редактирует** импортную строку месяца.
- Основной долг месяца считается как:  
  `debt = max(0, accrued - sum(paid по всем строкам месяца))`
- Метка «оплата за период» (`pay_for_period / use_period`):
  - не меняет дату фактической оплаты
  - не переписывает прошлые месяцы
  - влияет только на расчёт пени
- Запрещены ретро-изменения (anti‑retro):  
  оплата за прошлый период не должна пересчитывать пеню так, будто деньги поступили раньше.

**Где зафиксировано**
- `LOGIC_SPEC_v1.5.1.md` — раздел 4.4 (CRITICAL payments ledger)
- Подтверждено трассировкой `localStorage.setItem → payments_<LS>` из UI и импорта

---

> ⚠️ Любые изменения логики оплаты, импорта Excel, пени или отчётов  
> **обязаны** учитывать данную архитектуру.  
> Нарушение этих правил приведёт к юридически некорректному расчёту задолженности.


## v1.5.1 — DEV CRITICAL ASSERTS + Variant B wiring

- Добавлен dev-режим (включение: `?dev=1` или localhost) и helper `CRITICAL_ASSERT` (dev-only).
- `calc_engine.js`: добавлены `window.__DEV__` и `window.CRITICAL_ASSERT` (если не были определены ранее).
- `payment_table.js`: минимальный ранний возврат через `window.JKHCalcEngine.calcTotalsAsOfAdjusted` для 1:1 совпадения карточки и справки + dev-asserts на отрицательные долги.
- `abonent_card.html`: гарантирован порядок подключений `calc_engine.js` перед `payment_table.js`.
- `spravka_sud.html`: убрано подключение `storage.js` (ES-module) и добавлен `calc_engine.js` перед `spravka_sud.js`.
- `spravka_sud.js`: колонки «Месячная задолженность» теперь считаются строго за месяц (построчно), без накопления; итоговый блок внизу — нарастающий итог на конец месяца периода и должен совпадать с карточкой.
